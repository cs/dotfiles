conky.config = {
  background = false,
  out_to_console = true,
  out_to_x = false,
  update_interval = 1.0,
  total_run_times = 0,
  use_spacer = none,
  override_utf8_locale = true,
  template0 = [[^fn()^fg(\\#e2abd4)${time %Y-%m-%d %H:%M:%S}]]
};

conky.text = [[${template0}]];
