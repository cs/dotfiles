-- This is the currently active colorscheme, which can also be modified from the outside (e.g., the window manager). If
-- this happens, nvim has to be notified about it with SIGUSR1 (e.g., killall --quiet --signal SIGUSR1 nvim).
vim.cmd([[colorscheme carbonfox]])
