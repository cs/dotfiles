# Installation latest zsh on Mac OS X (>= 5.0.8):
#
#   brew install zsh --disable-etcdir
#
# Use zsh as login shell on Mac OS X:
#
#   sudo sh -c "echo '/usr/local/bin/zsh' >> /etc/shells"
#   chsh -s /usr/local/bin/zsh

export ZDOTDIR="$HOME/.zsh"
